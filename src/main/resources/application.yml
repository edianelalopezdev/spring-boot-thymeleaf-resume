spring:
  datasource:
    url: jdbc:h2:~/test;AUTO_RECONNECT=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true
  thymeleaf:
    cache: false

welcome:
  message: Mkyong
